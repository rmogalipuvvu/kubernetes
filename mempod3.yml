apiVersion: v1
metadata:
   name: constrains-mem-demo
spec:
   containers:
   - name: default-cpu-demo
     image: nginx
     resources:
       limits:
          memory: "800Mi"
       requests:
          memory: "100Mi"

